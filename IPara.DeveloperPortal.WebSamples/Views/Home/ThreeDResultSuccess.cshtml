@using System.Xml.Linq
@using IPara.DeveloperPortal.Core
@using Newtonsoft.Json

@model IPara.DeveloperPortal.Core.Response.ThreeDPaymentCompleteResponse


@{
    ViewBag.Title = "3D ile Ödeme Başarılı";
}

<h2>3D ile Ödeme Başarılı</h2>

<br />
<pre>
        @{
             if (Model!=null)
        {
             string formatted = XDocument.Parse(XmlBuilder.GetXMLFromObject(Model)).ToString();
               @Html.Raw(Html.Encode(formatted));
        }
     
            }
</pre>