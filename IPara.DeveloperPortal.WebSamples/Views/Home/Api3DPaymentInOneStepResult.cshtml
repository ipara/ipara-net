@using Newtonsoft.Json
@model IPara.DeveloperPortal.Core.Response.Api3DPaymentInOneStepResponse

@{
    ViewBag.Title = "Tek Adımda 3D ile Ödeme Sonuç Ekranı";
}

@if (Model != null && Model.Result == "1")
{
    <h2>3D ile Ödeme Başarılı!</h2>
}
else
{
    <h2>3D ile Ödeme Başarısız!</h2>
}


<br />
<pre>
         @{
          if (Model!=null)
            {
                @Html.Raw(JsonConvert.SerializeObject(Model, Formatting.Indented))
            }
     
            }
</pre>
