@using System.Xml.Linq
@using IPara.DeveloperPortal.Core
@using Newtonsoft.Json
@model IPara.DeveloperPortal.Core.Response.ThreeDPaymentInitResponse

@{
    ViewBag.Title = "3D ile Ödeme Yapılamadı";
}

<br />
<br />
<br />
<br />

<h2>3D ile Ödeme Başarısız!</h2>

<br />
<pre>
         @{
          if (Model!=null)
            {
                 string formatted = XDocument.Parse(XmlBuilder.GetXMLFromObject(Model)).ToString();
                   @Html.Raw(Html.Encode(formatted));
            }
     
            }
</pre>